# Sample environment file — do NOT commit real secrets to Git
# Copy this to .env and fill values before running the server.

# Supabase project URL (example: https://xyzabc.supabase.co)
SUPABASE_URL=

# Supabase server key for server-side use (service_role or anon depending on needs)
# Do NOT expose service_role in client-side/public repos.
SUPABASE_KEY=

# Vite / client-side keys (optional)
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=

# Admin auth token used by server API endpoints (choose a strong secret)
ADMIN_TOKEN=
